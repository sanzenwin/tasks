name: Generate chromium dist
on:
  push:
    tags:
    - tests-[0-9]+
permissions:
  contents: write
jobs:
  build-1:
    runs-on: macos-latest
    steps:
    - run: docker run --rm ubuntu df -h --total
    - run: df -h --total
