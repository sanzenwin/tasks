name: Generate chromium dist
on:
  push:
    tags:
    - tests-[0-9]+
permissions:
  contents: write
jobs:
  build-1:
    runs-on: windows-latest
    steps:
    - run: |
        where tar
        tar --version
      shell: cmd
  build-2:
    runs-on: macos-15
    steps:
    - run: |
        which tar
        tar --version
